在这个阶段，你将扩展实现以应对副本连接并收到写入命令的情况。WAIT

WAIT带有传播命令
在之前的阶段，我们处理了以下情况：

没有复制体连接，主人可以安全返回。0
副本是连接的，但没有收到任何写入命令。
现在，我们将处理写入命令被发送到副本的情况。由于复制偏移量不再是 ，主控需要在回复前检查哪些副本已成功处理最新的写入命令。0

为此，主控必须发送到副本，如果自上次以来有待处理的写入命令。每个副本将回复其当前偏移量（）。REPLCONF GETACK *WAITREPLCONF ACK <offset>

命令应在以下任一时间完成：WAIT

所需的副本数量已确认所有之前的写入命令，或
暂停时间到了。
无论哪种方式，主控都应以RESP整数返回确认所有之前写入命令的副本数量。

测试赛
测试器将以主控程序的形式执行，具体如下：

./your_program.sh
然后它会启动多个副本连接到你的服务器。每个人都会完成握手，并期望收到一个空的RDB文件。

接下来，测试器会以客户端身份连接到你的主控，并发送多个写入命令，这些命令交错排列：WAIT

$ redis-cli SET foo 123
$ redis-cli WAIT 1 500    # (must wait until either 1 replica has processed previous commands or 500ms have passed)

$ redis-cli SET bar 456
$ redis-cli WAIT 2 500    # (must wait until either 2 replicas have processed previous commands or 500ms have passed)
注释
返回的副本数量可能小于或多于命令中指定的预期副本数。WAIT